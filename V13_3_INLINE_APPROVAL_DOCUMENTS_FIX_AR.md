# تقرير باتش V13.3 — عرض مستندات الموافقات مباشرة

## المشكلة

في صفحة موافقات الفروع كانت المستندات تظهر كسطور تحتوي زر «عرض الملف»، بدل ظهور الصور مباشرة. وفي بعض سجلات موافقات المتاجر القديمة كانت الصور تظهر مباشرة لكن بدون أزرار قبول ورفض لكل ملف.

## التعديل

- توحيد عرض ملفات موافقات المتاجر والفروع بنفس الشكل.
- كل صورة تظهر مباشرة داخل كارت مستقل بدون الضغط على «عرض الملف» أولًا.
- ملفات PDF تظهر داخل معاينة مدمجة، مع زر فتح بالحجم الكامل.
- تحت كل ملف توجد أزرار:
  - فتح بالحجم الكامل.
  - قبول الملف.
  - رفض الملف.
- عرض حالة كل ملف: بانتظار المراجعة، مقبول، مرفوض، أو غير مرفوع.
- الملفات غير المرفوعة لا يمكن قبولها أو رفضها.
- دمج سجلات مراجعة الملفات الجديدة مع الصور الموجودة في حقول طلبات المتاجر والفروع، حتى لا تختفي واجهة الفرع أو السجل التجاري لمجرد وجود بعض سجلات المستندات فقط.
- دعم السجلات القديمة بدون Migration: عند قبول أو رفض صورة قديمة لأول مرة، ينشئ السيرفر سجل مراجعة لها من البيانات الأصلية الموثوقة ثم يسجل القرار.
- السيرفر لا يعتمد على مسار ملف يرسله المتصفح؛ بل يعيد قراءة مسار الملف من طلب المتجر أو الفرع في قاعدة البيانات قبل إنشاء سجل المراجعة.
- إضافة رسائل عربية وإنجليزية واضحة لو الملف غير مرفوع أو بيانات الطلب غير متاحة.

## الملفات المعدلة

- `src/components/data-section.tsx`
- `src/app/api/admin/action/route.ts`
- `src/app/globals.css`
- `src/lib/admin/messages.ts`
- `tests/admin-v13-3-inline-document-review.test.mjs`

## الاختبارات

- 109 اختبارًا ناجحًا.
- 0 اختبارات فاشلة.
- فحص تركيب ملفات TypeScript المعدلة نجح.
- تم التأكد من وجود حقول الصور المطلوبة في Views الخاصة بالمتاجر والفروع على Supabase.
- لم يتم تشغيل Migration أو تغيير مخطط قاعدة البيانات.

## طريقة الاستخدام

هذا الباتش يُفك فوق نسخة V13.2 الحالية مع الموافقة على استبدال الملفات.
