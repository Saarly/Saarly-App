# إصلاح بناء Vercel لتقييمات الدعم

تم إصلاح خطأ TypeScript الذي كان يظهر أثناء `next build` عند إنشاء خريطة التقييمات حسب `conversation_id`.

## السبب

TypeScript استنتج ناتج `ratingRows.map(...)` من الحقول المضافة فقط، لذلك لم يحتفظ بحقل `conversation_id` القادم من الصف الأصلي ضمن النوع المستنتج.

## الإصلاح

تم تعريف ناتج التحويل صراحةً كـ `AnyRow[]` وتعريف كل عنصر مرتجع كـ `AnyRow`، وبذلك يظل `conversation_id` وبقية حقول صف التقييم متاحة أثناء فحص الأنواع.

لم يتم تغيير منطق البيانات أو واجهة تقييمات الدعم.
