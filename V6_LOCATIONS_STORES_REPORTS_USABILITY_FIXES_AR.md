# تقرير مراجعة وإصلاح لوحة إدارة Saarly — V6

تاريخ المراجعة: 27 يوليو 2026

## النسخة التي تم العمل عليها

تم تعديل آخر نسخة مسلّمة:

`Admin_Web_OPERATIONAL_PRO_V6_FINAL_FIXED.zip`

ولم يتم نشر الملفات على Vercel ضمن هذه المراجعة.

## ما تم إصلاحه

### 1. المدن والمناطق

- استبدال الإضافة المباشرة لصفوف `cities` بدالة سيرفر محمية ومراجَعة.
- دعم إضافة وتعديل:
  - بلد.
  - محافظة.
  - مدينة.
- التحقق من الحقول المطلوبة ومنع تكرار نفس المكان.
- حفظ العملة على البلد وتطبيقها عبر منطق قاعدة البيانات الحالي.
- تسجيل الإنشاء والتعديل في سجل الإدارة.
- إضافة رسائل مفهومة بدل رسالة الخطأ العامة.

### 2. حذف المتجر

- المتجر الذي لا يملك طلبات أو سجلات مالية محفوظة يُحذف نهائيًا.
- المتجر الذي يملك تاريخ طلبات أو سجلات مالية يُحذف من التشغيل مع الاحتفاظ بالسجل المطلوب للمراجعة والمحاسبة.
- عند الحذف التشغيلي:
  - يختفي المتجر من مراجعة المتاجر والمنتجات وقوائم التشغيل.
  - يتم إيقاف المنتجات والفروع والموظفين.
  - تُسحب شارات المتجر.
  - لا تُحذف ملفات السجل التاريخي.
- الواجهة تعرض نتيجة واضحة تفرق بين الحذف النهائي والحذف مع حفظ السجل.

### 3. الشكاوى

- منع ظهور `not_provided` و`null` و`undefined` و`n/a` للمستخدم.
- عرض «غير متوفر» بالعربية و`Not provided` بالإنجليزية.
- تطبيق ذلك على اسم العميل ورقم الهاتف والمتجر والمسؤول عن الشكوى.

### 4. التقارير

- ترجمة القيم التقنية، ومنها:
  - `monthly_subscription` ← اشتراك شهري.
  - `commission` ← عمولة على المبيعات.
  - `tshirt` ← تيشيرت.
- إلغاء تصميم التقارير المزدحم وعرض تقرير واحد في كل مرة.
- إضافة اختيار نوع التقرير، وبحث داخل التقرير الحالي، وتنزيل التقرير كاملًا.
- إظهار عدد النتائج المطابقة الفعلي.
- عرض أول 10 سجلات مع زر واضح لعرض 10 إضافية، مع بيان «ظاهر X من Y».
- رفع الحد الداخلي للتقارير المحدودة من 10 إلى 1000 حتى لا يظهر عداد أكبر من البيانات المحمّلة.
- تحديد الحقول المفيدة لكل تقرير بدل عرض أسماء أعمدة تقنية للمستخدم.

### 5. شرح الصفحات والتابات

- إضافة صندوق شرح في بداية كل صفحة داخل لوحة الإدارة.
- الشرح بالعربية العامية المصرية في الواجهة العربية، وبالإنجليزية في الواجهة الإنجليزية.
- التابات المالية تحتفظ بشرح مستقل لكل تاب.
- أنواع التقارير تعرض شرحًا مستقلًا عند اختيار كل تقرير.

## تعديلات Supabase

تم تطبيق Migration الإنتاج التالية بنجاح:

`admin_v6_locations_and_store_archive_fix`

وتأكد وجود:

- `admin_upsert_city_location_as`.
- `admin_delete_merchant_as`.
- `admin_active_merchants_readable`.
- عمود `merchants.is_archived`.

ملف الـMigration موجود داخل النسخة للحفظ في Git فقط. لا تشغله يدويًا مرة أخرى على نفس مشروع Supabase.

## نتائج الفحص

- اختبارات المشروع: **37 من 37 ناجحة**.
- فحص تركيب TypeScript/TSX: **28 ملفًا، دون أخطاء تركيبية**.
- فحص Transpile المعزول: **28 ملفًا، دون أخطاء**.
- فحص الاستيرادات المحلية: **0 استيراد مفقود**.
- فحص أنماط الأسرار: **0 نتيجة**.
- فحص سلامة ZIP يتم بعد إنشاء الحزمة النهائية.

## قيد بيئة المراجعة

تعذر تنزيل حزم npm بسبب استجابة `503 Service Temporarily Unavailable` من مستودع الحزم داخل بيئة العمل، لذلك لم يتم تشغيل `next build` أو `tsc --noEmit` الكامل هنا. لم يتم إخفاء هذا القيد، وتم بدلًا منه تشغيل اختبارات المشروع وفحوص تركيب وترجمة TypeScript المستقلة. سيُنفذ Vercel البناء الكامل عند رفع النسخة.
