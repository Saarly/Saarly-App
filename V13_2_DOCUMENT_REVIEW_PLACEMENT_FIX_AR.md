# تقرير إصلاح مكان مراجعة مستندات المتاجر والفروع — V13.2

## التعديل المطلوب

تم تصحيح توزيع المسؤوليات بين الصفحات كالتالي:

- **موافقات المتاجر:** تعرض مستندات المتجر الأساسية مع حالة كل ملف وأزرار عرض الملف وقبول الملف ورفض الملف.
- **موافقات الفروع:** تعرض مستندات الفرع مع حالة كل ملف وأزرار عرض الملف وقبول الملف ورفض الملف.
- **المؤسسون والمتاجر:** بقي فيها زر عرض الملفات للرجوع إلى المستندات بعد اعتماد المتجر، لكن المستندات أصبحت للعرض والمتابعة فقط بدون قبول أو رفض.

## ما تم تغييره

1. تحميل سجلات `merchant_documents` مع صفوف موافقات المتاجر والفروع.
2. فصل صلاحية مراجعة مستندات المتجر عن مستندات الفرع.
3. استخدام الدالة الآمنة الموجودة في قاعدة البيانات لمراجعة المستندات بدل تعديل السجل مباشرة.
4. إزالة مسار مراجعة المستندات من صفحة تحقيق الدخل حتى لا توجد نفس العملية في مكانين.
5. إضافة توضيح داخل نافذة ملفات المتجر في صفحة المؤسسين بأنها للعرض فقط.
6. إبقاء السجلات القديمة التي لا تحتوي حالات مراجعة منفصلة قابلة للعرض مع توضيح حالتها.
7. إضافة تنسيق متجاوب لقائمة المستندات على الكمبيوتر والموبايل.

## الاختبارات

- `npm test`: **106 اختبارًا ناجحًا، 0 فشل**.
- فحص تركيب ملفات TypeScript/TSX المعدلة: ناجح.
- `npm ci` لم يكتمل في بيئة العمل بسبب عدم توفر حزمة داخل مخزن npm الداخلي، لذلك لم يتم تشغيل Build كامل هنا.

## طريقة التركيب

فك محتويات ملف الباتش فوق مشروع V13.1 الحالي مع الحفاظ على نفس المسارات والموافقة على استبدال الملفات.

لا توجد Migrations ولا تعديلات قاعدة بيانات ضمن الباتش.
