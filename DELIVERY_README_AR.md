# Admin Web — Operational Pro Upgrade

هذه النسخة متزامنة مع ترقية Supabase وبوابة المتاجر وتطبيق Flutter المرفقة في نفس التسليم.

## التركيب
1. احتفظ بنسخة من مجلد `Admin Web` الحالي باسم `Admin Web OLD`.
2. ضع هذا المجلد مكانه باسم `Admin Web`.
3. انقل ملف `.env.local` القديم فقط، ولا تنقل `.next` أو `node_modules`.
4. شغّل:
   - `npm ci`
   - `npm run typecheck`
   - `npm test`
   - `npm run build`

## ملاحظات
- لا تستخدم تبويب مستندات تحقيق الدخل؛ تمت إزالته من الواجهة.
- صفحتا المتاجر وسجل الحركات مخفيتان من التنقل والصلاحيات لتجنب التكرار.
- المستندات التاريخية غير الموجودة فعليًا ستظهر كـ«غير مرفوع» بدل صورة وهمية.
