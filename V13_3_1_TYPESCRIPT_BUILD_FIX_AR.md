# إصلاح Build لنسخة V13.3.1

## المشكلة

فشل بناء Next.js عند السطر الذي يقرأ `document.id` داخل قائمة مستندات الموافقات، لأن TypeScript استنتج نوعًا ضيقًا لبعض عناصر القائمة ولم يعتبرها `Row` كاملة.

## الإصلاح

تم تثبيت نوع ناتج قائمة المستندات صراحةً ليكون:

```ts
useMemo<Row[]>(...)
```

وبذلك تظل الحقول الديناميكية مثل `id` و`storage_path` و`storage_bucket` متاحة بصورة صحيحة أثناء البناء، بدون تغيير أي سلوك في الواجهة أو قاعدة البيانات.

## الملفات المعدلة

- `src/components/data-section.tsx`
- `tests/admin-v13-3-inline-document-review.test.mjs`

## التحقق

- اختبار TypeScript معزول لنفس نمط الاستدلال: ناجح.
- اختبارات المشروع: 110 ناجحة، 0 فشل.
- تمت إضافة اختبار انحدار يمنع حذف تعريف `Row[]` الصريح مستقبلًا.
- تعذر تشغيل `npm ci` والبناء الكامل داخل البيئة بسبب عدم توفر حزمة في مخزن npm الداخلي، وليس بسبب خطأ جديد في المشروع.

## طريقة الاستخدام

فك محتويات الباتش فوق مشروع V13.3 مع الحفاظ على المسارات، ووافق على استبدال الملفين.
