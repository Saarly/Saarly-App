# إصلاحات الأدمن والباك إند V7

الإصدار: `0.1.1`

## الفرع الرئيسي

- أول فرع في تسجيل المتجر يُعلّم كفرع رئيسي.
- الفرع الرئيسي لا يظهر في موافقات الفروع.
- قرار قبول أو رفض المتجر يطبّق على الفرع الرئيسي داخل نفس رحلة القرار.
- مراجعة الفرع الرئيسي بشكل منفصل تُرفض برسالة `primary_branch_reviewed_with_merchant`.
- الفروع الإضافية فقط تظل في قسم موافقات الفروع.

## صور المنتجات

ملف Migration يثبت عقد المسار المتفق عليه مع Flutter:

```text
<auth-user-id>/products/<merchant-id>/<file>
```

وسياسات الإدخال والقراءة والتعديل والحذف تتحقق من المستخدم الحالي والمتجر الحالي.

## البريد

لوحة الأدمن لم تعد تخفي فشل إرسال البريد بعد نجاح قرار القبول. عند رجوع التحذير `email_send_failed` يظهر للمشرف أن القرار وإشعار التطبيق حُفظا، لكن مزود البريد غير معد أو رفض الرسالة.

بيانات المزود المطلوبة موثقة في `EMAIL_PROVIDER_SETUP_REQUIRED_AR.md`.

## منع رجوع المشكلة

أضيف اختبار `admin-v7-regression-guard.test.mjs` يثبت:

- تطابق سياسة صور المنتجات مع التطبيق.
- إخفاء الفرع الرئيسي من موافقات الفروع.
- منع مراجعة الفرع الرئيسي منفصلًا.
- منع رسالة فرع مكررة للتسجيل الرئيسي.
- ظهور تحذير فشل مزود البريد في واجهة الأدمن.
