# تقرير الإصلاح النهائي للوحة إدارة سعرلي

تاريخ الإصدار: 30 يوليو 2026  
نسخة المشروع: `0.1.8`

## نطاق العمل

تم تنفيذ التعديلات على آخر نسخة رفعها المستخدم، بدون الرجوع إلى نسخة أقدم وبدون تغيير اتجاه الواجهة أو مكان القائمة الجانبية أو الهوية العامة.

## الإصلاحات والوظائف المضافة

### صفحة الطلبات

- استبدال العرض المختصر بواجهة تشغيل مخصصة للطلبات.
- إضافة بحث ومرشحات فعلية حسب حالة الطلب وحالة الدفع والمتجر والمشتري والفترة الزمنية.
- إضافة ترتيب حسب الأحدث والأقدم والأعلى قيمة والأقل قيمة.
- إضافة زر واضح لفتح تفاصيل الطلب.
- عرض بيانات المشتري والمتجر والطلب والتواريخ.
- عرض المنتجات والكميات وسعر الوحدة وإجمالي كل منتج.
- حساب وعرض قيمة المنتجات المختارة والشحن والإجمالي النهائي من البيانات المحفوظة فعليًا.
- عدم تسمية قيمة المنتجات المختارة بالإجمالي النهائي.
- عرض حالة الدفع بصياغة واضحة، ومنها «لا يحتاج دفعًا داخل التطبيق».
- إضافة تصدير حقيقي لبيانات الطلبات.

### شركات الشحن

- إضافة زر «عرض التفاصيل» لكل شركة.
- عرض حالة الشركة والمتجر المرتبط بها وعدد الشرائح وتاريخ الإنشاء وآخر تحديث.
- عرض شرائح الوزن والأسعار الفعلية الموجودة في قاعدة البيانات.
- توضيح أن الصفحة للمتابعة والقراءة عندما لا توجد إجراءات إدارية أخرى مسموحة.

### الدفع اليدوي

- إصلاح فتح إثبات الدفع من الحاوية الصحيحة `merchant-payment-proofs`.
- دعم ملفات الصور وPDF مع الحفاظ على الملف الأصلي دون ضغط.
- التفرقة بين عدم رفع إثبات ووجود مسار لملف مفقود وفشل إنشاء رابط الفتح.
- إضافة زر مستقل لتغيير الخطة قبل قبول الطلب.
- عرض الخطة الحالية والخطة الجديدة والسعر والفرق قبل التأكيد.
- منع تغيير الخطة بعد بدء المراجعة أو بعد قبول/رفض الطلب.
- ضمان تفعيل أو تمديد الاشتراك مرة واحدة فقط بعد القبول.

### البريد التلقائي

- توحيد الإرسال التلقائي بعد قرارات الإدارة مع نفس مسار «إعادة الإرسال» العامل.
- منع تسجيل «مزود البريد غير معد» بصورة عامة عندما يكون السبب الحقيقي مختلفًا.
- تحسين معالجة الخطأ وإرجاع السبب الحقيقي دون كشف بيانات سرية.
- إصلاح اسم المرسل العربي ليظهر `Saarly | سعرلي` بدون محارف تالفة.
- تنظيف عنوان واسم المرسل من محرف الاستبدال أو النص المشوه.
- تم نشر وظيفة البريد `process-admin-email-events` على Supabase بالإصدار 26.

### تحقيق الدخل

- إضافة حالة واضحة لكل وظيفة: «مفعلة» أو «غير مفعلة» بدل عرض تاريخ آخر تعديل فقط.
- إضافة حالة واضحة لطرق الدفع، مع حالة الاتصال والإعداد وبيئة البوابة عند توفرها.
- الحفاظ على تبويبات تحقيق الدخل واتجاهها وتصميمها الحالي.

### الوصول إلى الوظائف الموجودة

- مراجعة إجراءات الإدارة الموجودة في الخادم مقابل عناصر الواجهة.
- إضافة وصول واضح إلى مراجعة ملفات المتجر من شاشة مراجعة المتاجر والمنتجات.
- عدم إضافة وظائف وهمية أو أزرار لا يقابلها إجراء حقيقي في النظام.

### الصور والتحذيرات

- استبدال صور الواجهة بطريقة عرض محسنة تقلل حجم النقل وتحافظ على الشكل ونسبة الأبعاد.
- استخدام جودة متوازنة للصور العادية والشعارات.
- عدم ضغط إثباتات الدفع والمستندات وملفات PDF الأصلية.
- إزالة استخدام عناصر الصور العادية داخل مكونات الإدارة.
- تنظيم دوال تحميل البيانات لتكون ثابتة وتستخدم الاعتمادات الصحيحة.
- إزالة تعليقات تعطيل الفحص القديمة وعدم تعطيل قواعد الجودة.

## الملفات الرئيسية الجديدة

- `src/components/orders-console.tsx`
- `src/components/shipping-companies-console.tsx`
- `src/components/admin-image.tsx`
- `src/lib/admin/image-compression.ts`
- `tests/admin-final-operational-ui.test.mjs`

## الاختبارات والفحوصات

- اختبارات المشروع: **103 ناجحة، 0 فشل**.
- فحص اختلافات الملفات: لا توجد مسافات أو أخطاء دمج غير صالحة.
- فحص تركيب جميع ملفات TypeScript وTSX: **0 أخطاء تركيب**.
- لا توجد عناصر `<img>` عادية داخل `src`.
- لا توجد تعليقات `eslint-disable` داخل `src`.
- ملف ZIP تم فحص سلامته بعد الإنشاء.

## قيد بيئة الاختبار

تعذر تشغيل تثبيت الحزم وبناء Next الكامل داخل بيئة العمل الحالية بسبب خطأ 404 في مخزن الحزم الداخلي لحزمة خارج المشروع:

`zod-validation-error-4.0.2.tgz`

لذلك يجب تشغيل الأوامر التالية على جهاز المستخدم قبل الرفع إلى Vercel:

```bash
npm ci
npm run typecheck
npm run lint
npm test
npm run build
```

الاختبارات المستقلة الحالية نجحت، لكن لا يتم الادعاء بأن Build الكامل نُفذ داخل هذه البيئة.

## ما لم يتم اختباره فعليًا هنا

- فتح صندوق Gmail والتأكد بصريًا من معاينة الرسالة بعد الإصلاح.
- دفع إلكتروني حقيقي عبر بوابة في وضع الاختبار، لأن البوابة لم تُجهز بعد.

باقي التعديلات موجودة داخل نسخة المشروع المرفقة وجاهزة للتجربة المحلية والرفع بعد نجاح Build على جهاز المستخدم.
