# مراجعة وإصلاح لوحة إدارة سعرلي — V6

## إصلاح الإعلانات

كان إلغاء خيار «إعلان مستمر» ينشئ مدة جديدة تلقائيًا تبدأ وقت التعديل وتنتهي بعد 30 يومًا، لذلك كان الإعلان يظهر «شغال» رغم إزالة علامة الاستمرار.

تم تعديل قاعدة البيانات والواجهة كالتالي:

- حفظ آخر تاريخ بداية ونهاية محددين للإعلان قبل تحويله إلى مستمر.
- عند إلغاء «إعلان مستمر» تعود المدة الأصلية المحفوظة.
- إذا لم توجد مدة سابقة، يجب على الإدارة تحديد تاريخ البداية والنهاية بدل إنشاء مدة وهمية.
- الإعلان المنتهي يظهر «منتهي» ولا يدخل قائمة الإعلانات الشغالة.
- التطبيق يستبعد الإعلان إذا انتهى `ends_at` حتى لو كان سجل الإعلان مفعّلًا إداريًا.

تم إجراء اختبار حقيقي داخل معاملة مؤقتة ثم التراجع عنها، وكانت النتيجة:

- التحويل إلى مستمر: ناجح.
- استعادة المدة الأصلية: ناجحة.
- الحالة بعد الاستعادة: منتهي.
- الظهور في التطبيق: لا يظهر.

## مراجعة اللغات والنصوص

تمت مراجعة النصوص الثابتة والقيم المتغيرة في صفحات:

- تسجيل الدخول ولوحة التحكم.
- موافقات المتاجر والفروع.
- شركات الشحن والمستخدمون والفريق والصلاحيات.
- الأقسام والمدن والمناطق.
- مراجعة المتاجر والمنتجات والطلبات والتطابقات غير المؤكدة.
- قراءات المساعد الذكي.
- الدعم والشكاوى والتصنيفات.
- الإشعارات والإعلانات.
- معرفة المساعد ومراقبة المحتوى.
- التقارير وتحقيق الدخل والمدفوعات والإحالات.

أهم التعديلات:

- منع عرض رسائل تسجيل الدخول الخام القادمة من خدمة الدخول.
- ترجمة حالات الطلبات والموافقات والدفع والاشتراكات والشكاوى والأولوية والعمولات.
- ترجمة إجراءات سجل الحركات وأسماء الأقسام الداخلية بدل ظهور أسماء برمجية.
- تغيير زر اللغة من الاختصارات المختلطة إلى «الإنجليزية» و`Arabic`.
- ترجمة اسم الدور والقائمة والوضع الفاتح والداكن واسم الصفحة ووصفها.
- استبدال عبارات مثل «رمز الخطأ» و«كود العملة» بعبارات مفهومة.
- تغيير زر تنزيل التقرير من `CSV` إلى «تنزيل التقرير» / `Download report`.
- عرض العملة `ج.م` في العربي و`EGP` في الإنجليزي.
- إزالة أسماء خدمات الاستضافة وقاعدة البيانات من الرسائل الظاهرة للمستخدم.

ملاحظة: أسماء المتاجر والمنتجات والمستخدمين التي كتبها أصحاب الحسابات تظل كما أدخلوها؛ المراجعة تخص نصوص الواجهة وحالات النظام.

## مراجعة الأزرار والربط

- تم فحص 121 زرًا في مكونات لوحة الإدارة.
- لا يوجد زر بدون حدث تنفيذ أو إرسال نموذج مقصود.
- تم فحص 41 عملية يستدعيها جانب الواجهة، وكلها لها مسار تنفيذ في السيرفر.
- تم فحص إجراءات الجداول: العرض، القبول، الرفض، الحظر، فك الحظر، كلمة المرور، الحذف، التعديل، والتشغيل والإيقاف.
- تم إصلاح مرجع صف خاطئ في أزرار تعديل الدول والمحافظات كان سيؤدي إلى فشل فحص TypeScript.

## نتائج الاختبارات

- اختبارات المشروع: 29 من 29 ناجحة.
- فحص تركيب ملفات TypeScript وTSX: 27 ملفًا بدون أخطاء.
- الفحص الدلالي المحلي باستخدام تعريفات بديلة للمكتبات: صفر أخطاء.
- فحص أزرار الواجهة: 121 زرًا، صفر أزرار بلا ربط.
- فحص قاعدة البيانات للإعلان: ناجح وتم التراجع عن بيانات الاختبار.

تعذر تشغيل `next build` الكامل محليًا لأن حزم المشروع غير متاحة في بيئة التسليم. سيجري Vercel البناء الكامل بعد رفع النسخة.

## حالة Vercel التي أمكن التحقق منها

- الرابط العام `https://saarly-admin-web.vercel.app` يستجيب بنجاح.
- النسخة المرفوعة وقت الفحص كانت ما تزال تعرض عنوان الصفحة ووصفها القديمين بالإنجليزية؛ معنى ذلك أن إصلاحات V6 ليست منشورة قبل رفع هذا الباتش.
- الصفحات الداخلية محمية بتسجيل الدخول، لذلك لا يمكن تنفيذ ضغط فعلي شامل بحساب الإدارة من الفحص العام.
- مراجعة الأزرار تمت من الكود، ومسارات السيرفر، والصلاحيات، والاختبارات الآلية.

## مهم

تعديلات قاعدة البيانات الخاصة بالإعلانات مطبقة بالفعل على مشروع Supabase. لا تشغّل أي SQL ولا ترفع ملف SQL.
