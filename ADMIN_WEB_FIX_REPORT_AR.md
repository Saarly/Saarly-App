# تقرير إصلاح Admin Web — Saarly

## الحالة

تم إصلاح مسارات الإدارة الأساسية وربطها بطبقة Supabase الجديدة. النسخة جاهزة للاختبار بعد تطبيق Migration الإصلاح ونشر دالة البريد. لم يتم تعديل مشروع الإنتاج من هذه الحزمة.

## ما تم إصلاحه

### الموافقات والمستندات

- قبول ورفض المتجر يمر عبر `admin_review_merchant_registration_as` كعملية ذرّية.
- قبول ورفض الفرع يمر عبر `admin_review_branch_as`.
- قبول ورفض بطاقات المدير يمر عبر `admin_review_merchant_document_as`.
- منع مسار `update_row` العام من تغيير `approval_status` أو `rejection_reason`.
- منع الاعتماد على إخفاء أزرار الواجهة فقط؛ التحقق النهائي داخل قاعدة البيانات.

### إيقاف المتاجر

- فصل إيقاف المتجر عن حالة قبول التسجيل.
- الإيقاف وإعادة التفعيل يمران عبر `admin_set_merchant_suspension_as`.
- لا يتحول المتجر المعتمد إلى مرفوض عند إيقافه.
- كل تغيير يسجل سببه وفاعله في Audit Log.

### الاشتراكات والدفع اليدوي

- مراجعة طلب الدفع تستخدم عملية السيرفر الذرّية.
- منع تغيير الخطة بعد انتقال الطلب من `submitted` إلى `under_review`.
- عرض حالة البريد وعدد المحاولات وسبب الفشل في قسم أحداث الانتهاء.
- إعادة محاولة البريد تستدعي Edge Function الحقيقية بدل تعديل حالة السجل فقط.

### البريد

- ربط اللوحة بـ `process-admin-email-events` عبر `EMAIL_DISPATCH_SECRET`.
- توافق مفاتيح Idempotency بين اللوحة وقاعدة البيانات لمنع الرسائل المكررة.
- الإرسال الفوري الموجود في اللوحة يظل Fallback، والـOutbox تمنع النسخة الثانية.

### الاعتماديات والاختبارات

- تثبيت إصدارات الحزم بدل نطاقات الإصدارات المفتوحة.
- إضافة اختبارات ثابتة لمسارات الأمان.
- نجحت 7 اختبارات Node.
- نجح الفحص التركيبي لـ28 ملف TypeScript/TSX، وكل مسارات الاستيراد المحلية سليمة.

## ما يحتاج إعدادًا خارجيًا

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `EMAIL_DISPATCH_SECRET`
- إعداد Resend داخل Supabase Edge Function أو إعداد SMTP.
- لا يمكن تشغيل دفع إلكتروني حقيقي قبل اختيار مقدم دفع وتنفيذ Adapter الخاص به.

## نتيجة Build

تعذر تثبيت جميع حزم npm في بيئة الفحص بسبب توقف/تعليق اتصال مستودع الحزم. لذلك لا يتم الادعاء بأن `next build` اكتمل هنا. فحوصات التركيب والاختبارات المستقلة نجحت، ويجب تشغيل الأوامر التالية على جهازك بعد فك الملف:

```powershell
npm ci
npm run test
npm run typecheck
npm run lint
npm run build
```
