# إصلاحات لوحة إدارة Saarly — V4

## ما تم إصلاحه

- منع ظهور `action_failed` بعد نجاح قبول أو رفض متجر أو فرع. فشل إرسال البريد أو الإشعار بعد حفظ القرار أصبح تحذيرًا داخليًا ولا يحوّل العملية الناجحة إلى خطأ في الواجهة.
- جعل مساحة تصنيفات محادثة الدعم تتغير تلقائيًا حسب عدد صفوف التصنيفات بدل أن تملأ مساحة كبيرة من كارت المحادثة.
- إصلاح تحويل الإعلان من «إعلان مستمر» إلى إعلان محدد المدة. عند إلغاء الاختيار تُستعاد المواعيد السابقة، أو يقترح النظام بداية حالية ونهاية بعد 30 يومًا ويمكن تعديلهما.
- السماح بإيقاف عد المؤسسين مؤقتًا وإعادة تشغيله من لوحة الإدارة، مع الاحتفاظ بالأرقام الحالية.
- تحويل تعديل طريقة محاسبة المتجر إلى قائمة اختيار واضحة: «عمولة على كل عملية بيع» أو «اشتراك شهري».
- إضافة إعداد نسبة العمولة من: تحقيق الدخل ← العمولات. يدعم نسبة عامة ونسبة مختلفة حسب القسم.
- حساب العمولة على الطلبات المؤكدة الجديدة فقط، وللمتاجر المختارة لنظام العمولة فقط، مع استبعاد حسابات الاختبار.
- عند تحويل متجر إلى حساب اختبار: يُحذف رقم المؤسس وشارة المؤسس منه، ثم تُعاد أرقام المؤسسين التالية تلقائيًا بدون فجوات.

## حالة Supabase

تعديلات قاعدة البيانات مطبقة بالفعل على المشروع:

`fyffdppaujafpalzdcsa`

لا تشغّل ملفات SQL إضافية على المشروع الحالي.

## الرفع إلى GitHub

هذه الحزمة تبدأ مباشرة من جذر مشروع Next.js. بعد فكها سترى `package.json` و`src` و`public` مباشرة.

1. افتح الصفحة الرئيسية لمستودع `Saarly/Saarly-App`، وليس مجلد `src` أو `components`.
2. ارفع **محتويات** المجلد المفكوك من الجذر.
3. لا ترفع مجلد المشروع داخل `src/components`.
4. تأكد أن المسار الصحيح هو `src/components/monetization-console.tsx` وليس `src/components/src/...`.
5. نفّذ Commit على فرع `main` وانتظر نشر Vercel التلقائي.

## ملاحظات العمولة

- إعداد العمولة موجود داخل تبويب «العمولات» في صفحة «تحقيق الدخل».
- النظام حاليًا لا يبدأ خصم عمولات جديدة إلا بعد إدخال النسبة وتشغيل خيار «تشغيل حساب العمولة» وحفظه.
- تغيير النسبة لا يعيد حساب الطلبات القديمة؛ يطبق على الطلبات المؤكدة الجديدة فقط.
