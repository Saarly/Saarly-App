# تقرير باتش V13.4 — اعتماد الملفات واستبدال المرفوض

تاريخ التنفيذ: 30 يوليو 2026

## ما تم تعديله في الأدمن ويب

### 1. منع قبول المتجر قبل اعتماد ملفاته

عند محاولة قبول متجر، النظام يتحقق أولًا من الملفات المطلوبة الحالية للطلب. لا يتم قبول المتجر إذا:

- ملف مطلوب غير موجود أو لم تتم مراجعته بعد.
- يوجد ملف حالي مرفوض ولم يرفع صاحبه بديلًا له.

المنع موجود في زر الواجهة، وفي مسار الخادم، وداخل قاعدة البيانات نفسها؛ لذلك لا يمكن تجاوز الشرط بطلب مباشر.

### 2. منع قبول الفرع قبل اعتماد ملفاته

نفس القاعدة مطبقة على الفروع. الملفات الأساسية المطلوبة للفرع يجب اعتمادها أولًا، والسجل التجاري يصبح مطلوبًا للفرع الذي لا يستخدم سجل المتجر الرئيسي.

### 3. بريد تلقائي عند رفض ملف

عند رفض أي ملف من موافقات المتاجر أو الفروع:

- تحفظ حالة الملف كمرفوض مع السبب ووقت المراجعة والأدمن الذي راجعه.
- ينشأ سجل بريد واحد فقط لصاحب المتجر.
- يذكر البريد اسم الملف المرفوض، واسم المتجر أو الفرع، وسبب الرفض.
- يطلب من صاحب المتجر استبدال الملف من شاشة متابعة الطلب.
- يستخدم البريد نفس شعار وألوان وهوية سعرلي، ويدعم العربية والإنجليزية حسب لغة الحساب.
- مسار الإرسال هو نفس معالج البريد الذي يعمل مع إعادة الإرسال من لوحة الأدمن.

تم رفع وظيفة البريد الحية `process-admin-email-events` إلى الإصدار 27.

### 4. رسائل واضحة داخل الأدمن

بدل ظهور خطأ تقني، تظهر رسالة مفهومة توضح أن:

- الملفات المطلوبة يجب اعتمادها أولًا.
- أو الملفات المرفوضة يجب استبدالها أولًا.

ويظل قبول أو رفض الطلب بالكامل قرارًا منفصلًا عن قبول أو رفض ملف واحد.

## تعديلات قاعدة البيانات المطبقة حيًا

تم تطبيق الحماية على مشروع Supabase الحالي `fyffdppaujafpalzdcsa`، وتشمل:

- منع قبول متجر أو فرع قبل اعتماد الملفات المطلوبة.
- منع القبول في وجود ملف حالي مرفوض.
- فهارس لتحسين قراءة حالات الملفات وسجلات بريد رفض الملفات.

ملفات الترحيل موجودة داخل الباتش لحفظ تاريخ المشروع، ولا تحتاج إلى تشغيلها يدويًا على المشروع الحالي لأنها مطبقة بالفعل.

## الاختبارات

- اختبارات المشروع: 113 ناجحة، 0 فشل.
- فحص تركيب ملفات TypeScript المعدلة: ناجح.
- تم التحقق من أن الحماية موجودة داخل دوال قاعدة البيانات الحية للمتاجر والفروع.
- وظيفة البريد الحية: الإصدار 27 وحالتها ACTIVE.

## حدود التحقق

تعذر تشغيل `npm ci` وبناء Next الكامل في بيئة العمل الحالية لأن مخزن الحزم الداخلي لا يحتوي حزمة `zod-validation-error@4.0.2`. لذلك يجب تشغيل `npm run build` على جهازك بعد تركيب الباتش.

## قاعدة تركيب الباتش

يتم فك الباتش فوق نسخة V13.3.1 الحالية مع الحفاظ على مسارات المجلدات والموافقة على استبدال الملفات.
