# تقرير تسليم لوحة الإدارة V12

الإصدار: `0.1.6`

## التعديلات المنفذة

- استبدال أيقونة الاستضافة بأيقونة سعرلي في المتصفح وأجهزة Apple.
- إضافة قوالب محفوظة للإشعارات تشمل النصوص والجمهور والمستخدمين والوجهة والموقع الجغرافي، مع التحميل والحذف وإعادة الملء.
- ترتيب وجهات الإشعار: واجهات العملاء أولًا، ثم واجهات المتاجر، ثم دعوة الأصدقاء والوجهة المخصصة.
- تنظيف نصوص الإشعارات العربية من التشكيل المكرر والتطويل والمحارف المخفية قبل الحفظ، مع حماية إضافية داخل قاعدة البيانات وتنظيف السجلات القديمة.
- استبدال أزرار الإلغاء النصية داخل النوافذ بعلامة إغلاق أعلى يسار البطاقة.
- حذف العبارات التشغيلية غير المفيدة مثل «متصل بالبيانات المباشرة».
- إضافة حذف صلاحية عضو الإدارة/الدعم فقط، مع الحفاظ على حسابه العادي كعميل أو متجر وعدم حظره أو حذفه.
- استبدال جميع تصديرات CSV بتصدير XLSX حقيقي: رؤوس واضحة، التفاف نصوص، عرض أعمدة مناسب، تجميد الصف الأول، فلتر، واتجاه RTL للتقارير العربية.
- رفع تباين النصوص الثانوية في الوضع الفاتح.
- ضبط أماكن شريطي تمرير الصفحة والقائمة الجانبية في العربية والإنجليزية.
- إضافة قواعد قابلة للتعديل للفترة المجانية بحسب نطاق رقم المتجر، مع اختيار تطبيقها على المتاجر الحالية وحماية التمديدات اليدوية الأطول.
- توضيح حقول الطلبات: حالة الدفع داخل التطبيق، قيمة المنتجات المختارة، وحالة «لا يحتاج دفعًا داخل التطبيق».
- تحسين الأيقونات والنصوص المساعدة والاستجابات الثنائية اللغة المرتبطة بالتعديلات السابقة.

## تعديلات Supabase المطبقة بالفعل

تم تطبيق التعديلات التالية على مشروع الإنتاج `fyffdppaujafpalzdcsa`:

- جدول `admin_notification_templates` لحفظ قوالب الإشعارات.
- حماية تنظيف إشعارات الإدارة وتنظيف الإشعارات القديمة.
- قواعد `founder_trial_tiers` الافتراضية: من 1 إلى 100 = 180 يومًا، ومن 101 فما بعد = 60 يومًا.
- دالة تطبيق القواعد على المتاجر الحالية، متاحة لسيرفر لوحة الإدارة فقط.
- فهرس منشئ قالب الإشعار.

ملفات migrations موجودة داخل الحزمة للتوثيق ومزامنة المصدر فقط. **لا تُشغّلها مرة ثانية على مشروع Supabase الحالي.**

## نتائج الفحص

- اختبارات العقود والسلوك: **79 من 79 ناجحة**.
- فحص تركيب TypeScript/TSX: **29 ملفًا، دون أخطاء تركيبية**.
- مراجعة ربط إجراءات الواجهة بالسيرفر: يغطي الاختبار جميع الإجراءات النصية المستخدمة من الواجهة.
- فحص مكونات الواجهة: 132 زرًا و4 نماذج داخل ملفات المصدر المشمولة بالفحص.
- اختبار XLSX: ملف Open XML صحيح، اجتاز فحص ZIP، واتجاه الورقة العربية RTL.
- فحص قاعدة البيانات المباشر:
  - جدول قوالب الإشعارات موجود.
  - منظف الإشعارات يعمل.
  - قواعد الفترات موجودة.
  - المستخدم العادي لا يستطيع تشغيل إعادة احتساب الفترات؛ `service_role` فقط يستطيع.
  - النص القديم صاحب التشكيل المكرر أصبح محفوظًا بدون تشكيل متراكم.
- فحص أيقونات المتصفح: PNG وICO صالحان ومتعددا الأحجام.

## حدود الفحص داخل بيئة التسليم

تعذر تشغيل `next build` الكامل لأن مرآة npm الداخلية لم توفر الحزمة الخارجية `zod-validation-error@4.0.2`. لم يظهر خطأ مصدر من الملفات المعدلة، لكن لا يُسجل ذلك على أنه Build ناجح. عند رفع الحزمة إلى GitHub/Vercel سيُنفذ Vercel البناء بحزم npm المتاحة لديه.

ظهرت في Supabase Advisor ملاحظات قديمة تخص سياسات وفهارس جداول سابقة خارج نطاق هذه الدفعة. الجدول الجديد مقصود أن يكون Service-Role only، وتمت إضافة فهرس المفتاح الأجنبي الذي أشار إليه فحص الأداء.
