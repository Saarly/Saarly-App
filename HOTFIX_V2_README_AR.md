# Admin Web Operational Hotfix V2

تم تطبيق إصلاحات قاعدة البيانات مباشرة على مشروع Supabase:

`fyffdppaujafpalzdcsa`

لا تشغّل أي SQL إضافي لهذا الإصدار.

## تركيب لوحة الأدمن

1. احتفظ بالمجلد القديم باسم `Admin Web OLD`.
2. ضع هذا المجلد مكانه باسم `Admin Web`.
3. انسخ `.env.local` من النسخة القديمة إلى هذه النسخة.
4. تأكد أن بيئة Vercel تحتوي على نفس متغيرات Supabase، وبالأخص المفتاح السري المخصص للسيرفر. لا تضعه في متغير يبدأ بـ `NEXT_PUBLIC_`.
5. نفّذ:

```powershell
npm ci
npm run typecheck
npm test
npm run build
npm run dev
```

## الإصلاحات الرئيسية

- نقل قراءة لوحة التحكم والموافقات والكتالوج إلى API آمن يعمل من السيرفر.
- إنشاء روابط مؤقتة آمنة لصور Storage الخاصة بدل روابط `public` غير الصالحة.
- إلغاء اشتراط اعتماد صور الهوية من صفحة مستندات منفصلة قبل قبول أو رفض متجر أو فرع.
- إزالة البيانات التقنية من نافذة مراجعة الموافقات.
- تصحيح عرض البلاد والمحافظات والمدن في النسخة الإنجليزية.
- إصلاح صلاحيات تقارير المدفوعات والعمولات والمستحقات.
- تحسين تعيين موظف الدعم وإخفاء القائمة عندما لا يوجد موظفون.
- إضافة تصنيفات للشكاوى وإصلاح تسجيل الحل ونقلها إلى حالة تم الحل.
- إضافة اختيار صريح لإعلان مستمر دون تاريخ انتهاء.
- إصلاح بيانات المؤسسين والمتاجر وإضافة تعديل طريقة المحاسبة.

## ملاحظة عن الملفات القديمة

أي صورة موجودة فعليًا داخل Storage ستظهر برابط مؤقت آمن. بعض السجلات التجريبية والقديمة تشير إلى ملفات غير موجودة أصلًا داخل Storage؛ ستظهر لها رسالة واضحة بأن الصورة غير مرفوعة بدل فتح رابط 404.
